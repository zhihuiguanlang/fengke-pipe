server:
  port: 8892
  servlet:
    session:
      cookie:
        name: OAUTH2SESSION

spring:
  application:
    name: uaa-server

encrypt:
  key-store:
    location: classpath:zlt.jks
    secret: zlt!@#$
    alias: zlt